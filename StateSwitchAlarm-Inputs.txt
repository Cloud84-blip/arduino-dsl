sensor motionSensor at 11
alarm securityAlarm at 9

-> idle {
    motionSensor is HIGH => buttonDown
}

buttonDown {
    motionSensor is LOW => switchAlert
}

alertOn {
    securityAlarm <= HIGH
}

alertOn {
    securityAlarm <= LOW
}

switchAlert {
    securityAlarm is LOW => alertOn
    securityAlarm is HIGH => alertOff
}
